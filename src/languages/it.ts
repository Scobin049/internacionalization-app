import { LanguageListDTO } from './types';

const strings: LanguageListDTO = {
  hello: 'Ciao mondo',
  subtitle: 'Questa è la mia prima implementazione di un sistema multilingue',
  description:
    'Ho eseguito questo processo utilizzando la ContextAPI di React Native, è stato molto interessante utilizzare il contesto per gestire questa parte',
};

export default strings;
